# INTEGRATIONS

## External APIs
- None currently configured. The application functions as a static Single Page Application (SPA).

## Third-Party Services
- **Image Hosting:** Some images are fetched directly from external URLs (e.g., placeholder in `Hero.jsx` fetches from GitHub raw user content). This should be localized or properly managed via a CDN if intended for production.
